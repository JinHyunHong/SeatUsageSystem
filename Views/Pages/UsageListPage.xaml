<Page
  x:Class="SeatUsageSystem.Views.Pages.UsageListPage"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:local="clr-namespace:SeatUsageSystem.Views.Pages"
  xmlns:helpers="clr-namespace:SeatUsageSystem.Helpers"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
  Title="UsageListPage"
  d:DataContext="{d:DesignInstance local:UsageListPage,
                                     IsDesignTimeCreatable=False}"
  d:DesignHeight="450"
  d:DesignWidth="800"
  ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
  ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
  Foreground="{DynamicResource TextFillColorPrimaryBrush}"
  mc:Ignorable="d"
 >
    <Grid>
        <Grid VerticalAlignment="Top">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Orientation="Horizontal" Grid.Column="0">
                <DatePicker SelectedDate="{Binding ViewModel.StartDate, Mode=TwoWay}" Width="205" Opacity="1"/>
                <TextBlock Text="~" VerticalAlignment="Center" Margin="5,0"/>
                <DatePicker SelectedDate="{Binding ViewModel.EndDate, Mode=TwoWay}"  Width="205" Opacity="1"/>
            </StackPanel>

            <Button Grid.Column="2"
            Content="조회"
            Width="120"
            Height="35"
            Background="DarkSlateGray"
            Foreground="WhiteSmoke"
            FontWeight="Bold"
            Command="{Binding ViewModel.SearchCommand}" />
        </Grid>

        <Grid Margin="0,50,0,0">
            <!-- DataGrid -->
            <DataGrid x:Name="grdUsage"
                  ItemsSource="{Binding ViewModel.UsageListDtos, Mode=OneWay}"
                  IsReadOnly="True"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  GridLinesVisibility="All"
                  RowHeight="40"
                  HorizontalGridLinesBrush="#FFB0B0B0"
                  VerticalGridLinesBrush="#FFB0B0B0"
                  Grid.Column="0">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="이름" Binding="{Binding MemberName}" Width="120"/>
                    <DataGridTextColumn Header="연락처" Binding="{Binding Phone, Converter={StaticResource PhoneNumberConverter}}" Width="150"/>
                    <DataGridTextColumn Header="좌석" Binding="{Binding SeatLabel}" Width="80"/>
                    <DataGridTextColumn Header="상태" Binding="{Binding Status}" Width="100"/>
                    <DataGridTextColumn Header="시작일시" Binding="{Binding StartAt, StringFormat={}{0:yyyy-MM-dd HH:mm}}" Width="150"/>
                    <DataGridTextColumn Header="종료일시" Binding="{Binding EndAt, StringFormat={}{0:yyyy-MM-dd HH:mm}}" Width="150"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Grid>
</Page>
