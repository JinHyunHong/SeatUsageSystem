<ui:FluentWindow
    x:Class="SeatUsageSystem.Views.Windows.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="clr-namespace:SeatUsageSystem.Views.Windows"
    xmlns:pages="clr-namespace:SeatUsageSystem.Views.Pages"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    mc:Ignorable="d"

    Title="{Binding ViewModel.ApplicationTitle, Mode=OneWay}"
    Width="950"
    Height="900"

    d:DataContext="{d:DesignInstance local:MainWindow, IsDesignTimeCreatable=True}"
    d:DesignHeight="450"
    d:DesignWidth="800"

    ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
    ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"

    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    WindowBackdropType="Mica"
    WindowCornerPreference="Round"
    WindowStartupLocation="CenterScreen"
    ExtendsContentIntoTitleBar="True">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- TITLE BAR -->
        <ui:TitleBar
            x:Name="TitleBar"
            Grid.Row="0"
            Title="{Binding ViewModel.ApplicationTitle}"
            CloseWindowByDoubleClickOnIcon="True">

            <ui:TitleBar.Icon>
                <ui:ImageIcon Source="pack://application:,,,/Assets/wpfui-icon-512.png" />
            </ui:TitleBar.Icon>

        </ui:TitleBar>

        <!-- NAVIGATION -->
        <ui:NavigationView
            x:Name="RootNavigation"
            Grid.Row="1"
            Padding="42,0,42,0"
            FrameMargin="0"
            IsBackButtonVisible="Visible"
            IsPaneToggleVisible="True"
            PaneDisplayMode="Top"
            MenuItemsSource="{Binding ViewModel.MenuItems, Mode=OneWay}"
            TitleBar="{Binding ElementName=TitleBar, Mode=OneWay}">

            <!-- HEADER -->
            <ui:NavigationView.Header>
                <Grid Height="25"/>
            </ui:NavigationView.Header>

            <!-- FOOTER -->
            <ui:NavigationView.FooterMenuItems>

                <ui:NavigationViewItem
                    Content="{Binding ViewModel.LoginMenuText}"
                    Icon="{ui:SymbolIcon Symbol=Person24}"
                    TargetPageType="{x:Type pages:LoginPage}" />

                <ui:NavigationViewItem
                    Content="설정"
                    Icon="{ui:SymbolIcon Symbol=Settings24}"
                    TargetPageType="{x:Type pages:SettingsPage}" />

            </ui:NavigationView.FooterMenuItems>

            <!-- OVERLAY -->
            <ui:NavigationView.ContentOverlay>
                <Grid>
                    <ui:SnackbarPresenter x:Name="SnackbarPresenter" />
                </Grid>
            </ui:NavigationView.ContentOverlay>

        </ui:NavigationView>

    </Grid>

</ui:FluentWindow>